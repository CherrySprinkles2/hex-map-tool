/**
 * The URL path prefix the app is served from (mirrors the `homepage` field in
 * package.json / the PUBLIC_URL environment variable set by CRA).
 * Used in tests that navigate directly to a URL rather than using click helpers.
 */
export const APP_BASE = '/hex-map-tool';
